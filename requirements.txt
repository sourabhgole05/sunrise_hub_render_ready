# Sg_ent_media_radio uses Python standard library only.
#
# Optional feature integrations. The dashboard keeps its standard-library
# server and falls back to its existing public APIs when these are unavailable.
ffmpeg-python>=0.2.0
internetarchive>=5.0.0
feedparser>=6.0.11
fastapi>=0.115.0
uvicorn>=0.30.0
movie-box-dl>=2.0.2,<2.1
