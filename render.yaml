services:
  - type: web
    name: sunrise-hub-iptv
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python iptv_dashboard.py
    healthCheckPath: /healthz
    autoDeploy: true
